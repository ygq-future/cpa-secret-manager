// Package version holds the plugin release version. It is the single source
// shared by the host registration metadata, the embedded page badge and the
// release gates, and must stay in sync with registry.json.
package version

// PluginVersion is the plugin release version.
const PluginVersion = "1.0.1"
